# Content Rules

1. Do not edit normative wording without explicit approval.
2. Preserve chapter `id` values because navigation, search, and print references depend on them.
3. Use semantic headings and keep one `h1` per page.
4. Use `button` for actions and `a` for navigation.
